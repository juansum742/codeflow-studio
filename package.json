{
  "name": "codeflow-studio-site",
  "private": true,
  "type": "module",
  "description": "Official website and CRM frontend for CodeFlow Studio",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build-site.mjs",
    "deploy": "git push origin main",
    "dev:backend": "npm --prefix backend run dev"
  }
}
